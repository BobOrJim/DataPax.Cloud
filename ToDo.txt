



IMORGON ÄR DET UTSEENDE SOM GÄLLER, samt inlämings uppgiften.
* bilder på knappar
* ändra namn på knappar så de inte heter knapp1 osv.
* Kanske lägga till färgglidande bakgrund, så som jag gjorde på IQR. "Radial-gradient" gör det snyggt som fan. Kanske kan matchas med opage eller hur det nu stavas.



@page "/"
@page "/customer/{CustomerId}"
<h1>
  Customer:
  @if (string.IsNullOrEmpty(CustomerId))
  {
    @:None
  }
  else
  {
    @CustomerId
  }
</h1>
<h3>Select a customer</h3>
<ul>
  <li><a href="/customer/Microsoft">Microsoft</a></li>
  <li><a href="/customer/Google">Google</a></li>
  <li><a href="/customer/IBM">IBM</a></li>
</ul>
@code {
  [Parameter]
  public string CustomerId { get; set; }
}






//Denna är jäkligt lättjobbad. Undra vad dess prestanda är.
//Jag skulle kunna kolla för python
for (int y = 0; y < height; y++)
{
    for (int x = 0; x < width; x++)
    {
        pix1 = src.GetPixel(x, y);
        pix2 = comp.GetPixel(x, y);
        if (pix1 != pix2)
        {
            dest.SetPixel(x, y, pix1);
        }
    }
}






* INTE LÄGGA TILL NÅGOT MER PÅ LISTAN
* Bild stacken skall sorteras efter start och stop time
* URL timer grejen.
* Det hade varit läckert med picture filter.....rörelse alltså. då får jag även min första useCase + att jag kan använda Domain.
* Börja tänka mer på publiceringen.






UPPDATERINGS HIRARKI:
TriggerListClick ->
	GlobalTime time grejor
	Update spinboxes 1-6 ->
		GlobalTime grejor
		Update graph
		Update picture stack (med tidstämpel LastUpdateStartStop property för att undvika 6st updates)
Half Forw etc
	Update spinboxes

###############################################################################################################################


Inför storstädningen:
* Fyll detta prog med try/catch. och sök på det, just nu finns det ingenting. Gör fil för fil på slutet så jag inte missar något.
* OBS: kanske kan alla external uppdate utgå...städa kod...
* Städa bort template kod.




TROLIGEN UTGÅR:
* plotta analoga signaler (tror denna utgår, det kommer ändå inte bli snyggt)
* fråga jonathan efter kontakt till yrkeslivet, visa film och fråga...

